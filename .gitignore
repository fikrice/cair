# Log files
*.log

# OS generated files
.DS_Store
Thumbs.db

# Environment files
.env
.env.backup
.env.production

# Dependency directories
/node_modules
/vendor

# IDE & Editor files
/.fleet
/.idea
/.nova
/.vscode
/.zed
.phpactor.json

# Testing & Cache
.phpunit.result.cache
/.phpunit.cache

# Laravel specific
/storage/*.key
/storage/pail
/public/hot
/public/storage
Homestead.json
Homestead.yaml

# Vite Compiled Assets
# Commented out so compiled production assets (CSS/JS) can be pushed to GitHub 
# and pulled directly on Hostinger (no need to run npm run build on Hostinger server)
# /public/build
